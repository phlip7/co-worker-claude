# Experiment: REST API Ingestion to GCP

## Objective
Ingest source data from REST API and load into GCP bucket (bronze layer, medallion architecture).

## Infrastructure
- **Execution**: GCP Dataproc
- **Orchestration**: GCP Composer (Airflow)
- **Processing**: PySpark
- **Storage**: GCS bucket (bronze layer)
- **Deployment**: Git Actions → Wheel → Docker → Container execution (FYI only)

## Requirements

### Core Features
- Initial and incremental loads
- Idempotent operations
- Rerun, backfill, and late arriving data handling
- Single table or all tables ingestion (parameter-driven)
- Reference table handling (full snapshot for tables without change tracking)
- Pagination handling
- Schema validation
- Error handling and logging

### Technical Standards
- Python 3.x with PySpark
- Type hints and docstrings (PEP 8)
- Config-driven (no hardcoded values)
- Comprehensive error handling and logging
- Unit tests for core functions

## Repository Structure
```
experiment/rest-api-ingestion/
├── airflow/                    # Airflow DAGs and configs
├── analytique/
│   ├── common/                # SHARED: Generic reusable modules
│   │   ├── api/
│   │   │   └── client.py      # Generic API client (any REST API)
│   │   ├── storage/
│   │   │   └── gcs_writer.py  # Generic GCS writer
│   │   └── utils/
│   │       ├── logger.py      # Logging setup
│   │       └── helpers.py     # Common utilities
│   └── msb_ingestion/         # PROJECT-SPECIFIC: This ingestion project
│       ├── ingestion/
│       │   ├── processor.py   # MSB-specific transformations
│       │   └── rules.py       # MSB-specific ingestion rules
│       └── config/
│           └── settings.py    # MSB-specific configuration
├── main.py                     # Orchestrator (tells ingestion story)
├── tests/                      # Unit and integration tests
├── scripts-gcp/               # GCP resource creation scripts
├── docs/                      # Documentation
├── README.md
└── pyproject.toml
```

## Job Parameters
The job accepts CLI arguments for flexible execution:
- `--table`: Specific table name or "all" for all tables
- `--mode`: initial, incremental, backfill, or reference
- `--start-date`: Start date for data extraction (YYYY-MM-DD)
- `--end-date`: End date for data extraction (YYYY-MM-DD)
- `--config-path`: Path to config file (optional)

Example: `python main.py --table customers --mode incremental --start-date 2024-01-01`

## Main.py Flow (Story Structure)
1. Parse CLI parameters
2. Initialize logger (from common.utils)
3. Load configuration (project-specific)
4. Determine tables to process (single or all)
5. For each table:
   - Fetch data using generic API client (common.api.client)
   - Determine ingestion mode (project-specific rules)
   - Transform and validate data (project-specific processor)
   - Write to GCS using generic writer (common.storage.gcs_writer)
6. Log completion summary

## Design Principles
- **Separation**: Generic components in `common/`, project-specific in `msb_ingestion/`
- **Reusability**: API client and storage modules usable across all ingestion projects
- **Extensibility**: New ingestion projects add their own folder under `analytique/`
- Medallion architecture (bronze layer focus)
- Partition by ingestion date
- Idempotency via unique identifiers and timestamps
- Incremental logic based on watermarks/checkpoints for transactional tables
- Full snapshot approach for reference tables
- Table metadata driven from configuration
